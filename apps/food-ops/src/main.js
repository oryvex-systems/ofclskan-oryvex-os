import { createClient } from '@supabase/supabase-js'
import './style.css'

const supabase=createClient('https://wdimzayfvtlrxljpsvza.supabase.co','sb_publishable_FZwX09JGrJt3Q9WXW3V1dQ_-g9aegh4')
const app=document.querySelector('#app')
const money=v=>`${Number(v||0).toLocaleString('tr-TR',{minimumFractionDigits:2,maximumFractionDigits:2})} ₺`
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))
let state={user:null,sellers:[],orders:[],tasks:[],incidents:[],stock:[],activeSeller:'all'}

async function boot(){const {data:{session}}=await supabase.auth.getSession();state.user=session?.user||null;if(!state.user)return login();await load();render()}
async function login(){app.innerHTML=`<main class="login"><section><div class="x">X</div><h1>ORYVEX FOOD OPS</h1><p>TIKLADOY + BURGERMY işletim ve takip merkezi</p><input id="email" type="email" placeholder="E-posta"><input id="password" type="password" placeholder="Şifre"><button id="login">Giriş Yap</button><small id="status"></small></section></main>`;document.querySelector('#login').onclick=async()=>{const email=document.querySelector('#email').value.trim(),password=document.querySelector('#password').value;const {error}=await supabase.auth.signInWithPassword({email,password});if(error){document.querySelector('#status').textContent=error.message;return}boot()}}
async function load(){const {data:members}=await supabase.from('seller_members').select('seller_id,role,sellers(id,name,slug)').eq('user_id',state.user.id).eq('is_active',true);state.sellers=(members||[]).map(m=>({...m.sellers,role:m.role})).filter(Boolean);const sellerIds=state.sellers.map(s=>s.id);if(!sellerIds.length)return;const [{data:orders},{data:tasks},{data:incidents},{data:stock}]=await Promise.all([supabase.from('orders').select('id,order_no,seller_id,status,payment_status,payment_method,total_amount,created_at,branch_id').in('seller_id',sellerIds).order('created_at',{ascending:false}).limit(100),supabase.from('ops_tasks').select('*').in('seller_id',sellerIds).order('created_at',{ascending:false}).limit(100),supabase.from('ops_incidents').select('*').in('seller_id',sellerIds).order('created_at',{ascending:false}).limit(100),supabase.from('ops_stock_items').select('*').in('seller_id',sellerIds).eq('is_active',true).order('name')]);state.orders=orders||[];state.tasks=tasks||[];state.incidents=incidents||[];state.stock=stock||[]}
const filtered=a=>state.activeSeller==='all'?a:a.filter(x=>x.seller_id===state.activeSeller)
function render(){const orders=filtered(state.orders),tasks=filtered(state.tasks),incidents=filtered(state.incidents),stock=filtered(state.stock);const today=new Date().toISOString().slice(0,10),todayOrders=orders.filter(o=>o.created_at?.slice(0,10)===today),revenue=todayOrders.filter(o=>o.payment_status==='paid'||o.status==='delivered').reduce((s,o)=>s+Number(o.total_amount||0),0),open=orders.filter(o=>!['delivered','cancelled'].includes(o.status)).length,critical=incidents.filter(i=>i.status!=='resolved'&&i.status!=='closed'&&['high','critical'].includes(i.severity)).length,lowStock=stock.filter(s=>Number(s.current_qty)<=Number(s.minimum_qty)).length;app.innerHTML=`<div class="shell"><aside><div class="brand"><span>X</span><div><b>ORYVEX</b><small>FOOD OPS</small></div></div><button class="nav active">⌂ Genel Bakış</button><button class="nav">▤ Siparişler</button><button class="nav">☑ Görevler</button><button class="nav">⚠ Olaylar</button><button class="nav">◫ Stok</button><button class="nav">₺ Finans</button><div class="brands">${['all',...state.sellers.map(s=>s.id)].map(id=>{const s=state.sellers.find(x=>x.id===id);return `<button data-seller="${id}" class="${state.activeSeller===id?'sel':''}">${id==='all'?'TÜM MARKALAR':esc(s?.name)}</button>`}).join('')}</div></aside><main><header><div><small>İŞLETİM MERKEZİ</small><h1>TIKLADOY + BURGERMY</h1></div><div><button id="refresh">↻</button><button id="logout">Çıkış</button></div></header><section class="metrics"><article><span>Bugünkü Sipariş</span><b>${todayOrders.length}</b></article><article><span>Açık Sipariş</span><b>${open}</b></article><article><span>Bugünkü Ciro</span><b>${money(revenue)}</b></article><article><span>Kritik Olay</span><b>${critical}</b></article><article><span>Düşük Stok</span><b>${lowStock}</b></article></section><section class="grid"><article class="panel"><div class="ph"><h2>Canlı Siparişler</h2><span>${open} açık</span></div>${orders.slice(0,12).map(o=>`<div class="row"><div><b>${esc(o.order_no)}</b><small>${sellerName(o.seller_id)} · ${new Date(o.created_at).toLocaleTimeString('tr-TR',{hour:'2-digit',minute:'2-digit'})}</small></div><span class="badge ${o.status}">${labelStatus(o.status)}</span><strong>${money(o.total_amount)}</strong></div>`).join('')||'<p class="empty">Sipariş yok.</p>'}</article><article class="panel"><div class="ph"><h2>Operasyon Görevleri</h2><button id="newTask">+ Görev</button></div>${tasks.filter(t=>!['done','cancelled'].includes(t.status)).slice(0,10).map(t=>`<div class="task"><i class="${t.priority}"></i><div><b>${esc(t.title)}</b><small>${esc(t.category)} · ${esc(t.status)}</small></div></div>`).join('')||'<p class="empty">Açık görev yok.</p>'}</article><article class="panel"><div class="ph"><h2>Olay / Sorun Takibi</h2><span>${incidents.filter(i=>!['resolved','closed'].includes(i.status)).length} açık</span></div>${incidents.filter(i=>!['resolved','closed'].includes(i.status)).slice(0,8).map(i=>`<div class="task"><i class="${i.severity}"></i><div><b>${esc(i.title)}</b><small>${esc(i.type)} · ${esc(i.status)}</small></div></div>`).join('')||'<p class="empty">Açık olay yok.</p>'}</article><article class="panel"><div class="ph"><h2>Stok Uyarıları</h2><span>${lowStock} kritik</span></div>${stock.filter(s=>Number(s.current_qty)<=Number(s.minimum_qty)).slice(0,8).map(s=>`<div class="row"><div><b>${esc(s.name)}</b><small>${sellerName(s.seller_id)}</small></div><strong>${Number(s.current_qty)} ${esc(s.unit)}</strong></div>`).join('')||'<p class="empty">Stok uyarısı yok.</p>'}</article></section></main></div>`;document.querySelectorAll('[data-seller]').forEach(b=>b.onclick=()=>{state.activeSeller=b.dataset.seller;render()});document.querySelector('#refresh').onclick=async()=>{await load();render()};document.querySelector('#logout').onclick=async()=>{await supabase.auth.signOut();state.user=null;login()};document.querySelector('#newTask')?.addEventListener('click',newTask)}
function sellerName(id){return state.sellers.find(s=>s.id===id)?.name||'Marka'}
function labelStatus(s){return({received:'Yeni',preparing:'Hazırlanıyor',courier_assigned:'Kurye Atandı',on_the_way:'Yolda',delivered:'Teslim',cancelled:'İptal'})[s]||s}
async function newTask(){if(!state.sellers.length)return;const title=prompt('Görev başlığı');if(!title)return;const sellerId=state.activeSeller==='all'?state.sellers[0].id:state.activeSeller;const {error}=await supabase.from('ops_tasks').insert({seller_id:sellerId,title,priority:'medium',status:'todo',category:'operation',created_by:state.user.id});if(error)return alert(error.message);await load();render()}

supabase.auth.onAuthStateChange(()=>{});boot()
